# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/metodo_forum

# Authentication (Better Auth)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-key-here

# Email (Resend)
RESEND_API_KEY=your-resend-api-key-here

# Email autorisé en développement (plan gratuit Resend)
# Utilisez l'email associé à votre compte Resend pour tester en local
RESEND_DEV_EMAIL=your-resend-email@example.com

# Email expéditeur (affiché dans le "From:")
EMAIL_FROM=onboarding@resend.dev

# URL de l'application
NEXT_PUBLIC_APP_URL=http://localhost:3000